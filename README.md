# ecs-189g-project
factcheckmate

## Git LFS required

This repo stores large `.pth` and dataset files via Git LFS.  
Before cloning or pulling, install Git LFS and run:

```bash
git lfs install
```

If you already cloned and files are missing:
```bash
git lfs pull
```
